import { BambiiDirective } from './bambii.directive';

describe('BambiiDirective', () => {
  it('should create an instance', () => {
    const directive = new BambiiDirective();
    expect(directive).toBeTruthy();
  });
});
